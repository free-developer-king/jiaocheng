{
  "name": "ai-longge-tutorial",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html styles.css app.js dist/ && cp -R assets dist/assets"
  },
  "devDependencies": {}
}
